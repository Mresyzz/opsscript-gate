# Basic POSIX Setup Example

This example demonstrates a clean, POSIX-compliant shell script that runs without issues across all supported Linux distributions.

## How to run

```bash
opsscript-gate run ./examples/basic/clean_setup.sh
```

## Expected Outcome

All 4 default distributions will report `PASS` with exit code `0`.
